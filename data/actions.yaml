# Player actions. Each record colocates the cost, cooldown, event probability,
# formula numbers, and message pools for one action. Game logic in
# `src/game/actions.ts` reads from `ACTION[id]`; UI gates use ids consistent
# with `actionCooldowns` keys.
#
# Cross-cutting numbers (THRESHOLDS, HYPE display thresholds, MONEY, UPTIME,
# STREAMING, save/theme keys, agent buff bug-rate multiplier) remain in
# `src/game/constants.ts`.

- id: prompt
  tokenCost: 7
  cooldownMs: 5000
  # Random events after earlyPromptMsgs; decay see PROMPT_EVENT in constants.ts.
  eventProbability: 0.12
  earlyPromptMsgs:
    - |-
      > build me a startup. make no mistakes
      Certainly! I'd be happy to help with that.
    - |-
      > you just made a mistake
      You're absolutely right. I apologize. I'll make no further mistakes.
    - |-
      > ...is it running?
      Great question! Let me delve into that for you.

- id: kick_agent
  tokenCost: 60
  buffMs: 30000
  eventProbability: 0.2
  introMsg: "I'd be happy to kick off a subagent to parallelize this work."
  messages:
    - |-
      > kick off an agent
      Agent initialized! It's quietly reading all {{rand 400 1200}} files first, for context. It will begin shortly.
    - |-
      > spawn an agent
      Agent launched! It's spinning up subprocesses and downloading the full repository for comprehensive context.
    - |-
      > run the agent
      Agent active! It's opened {{rand 6 21}} tabs and is thinking carefully. Output incoming.
    - |-
      > go agent go
      Agent deployed! It has enabled computer use. I'd rather not say what it clicked.
    - |-
      > agent mode
      Agent running! It refactored {{rand 2 6}} files outside the requested scope. Very thorough.

- id: paste_error
  tokenCost: 10
  cooldownMs: 4000
  logCooldownMs: 12000
  fixChance: 0.5
  baseLocGain: 20
  extraLocRange: 30
  introMsg: "When something breaks, paste the error back in — I'd be happy to investigate."
  goodMessages:
    - |-
      > why is this failing
      I see the issue — it was an off-by-one error. Here's the corrected version.
    - |-
      > here's the stack trace
      Got it. The root cause was a missing null check. Fixed — should be cleaner now.
    - |-
      > it keeps crashing
      I've identified the issue. It was the code I wrote last session. Corrected.
    - |-
      > same error again
      I see the problem — I was importing the wrong module. Simple fix, done.
    - |-
      > this broke after your last change
      You're absolutely right — I introduced that. My apologies. Here's the corrected version.
  badMessages:
    - |-
      > it's still broken
      I see the issue! Let me fix that.
      > same error
      Interesting. I see a different issue now. Let me fix that instead.
    - |-
      > here's the error
      I see the issue — it's actually caused by the fix I just applied. I'll address that now.
    - |-
      > this doesn't work
      Absolutely! I've refactored the entire function for clarity. The error is different now.
    - |-
      > this broke after your last change
      One of the twelve TODOs was `// TODO: handle null data` at line 41. I have now handled it. I am so sorry.
    - |-
      > still getting this
      I've resolved the underlying issue and refactored the surrounding code for improved readability.
  neutralMessages:
    - |-
      > here's the error
      This looks correct to me. The behavior you're experiencing may be environmental.
    - |-
      > it's broken
      Have you tried clearing the cache? I've also made a small adjustment — probably unrelated.
    - |-
      > error message below
      I don't see an issue with the code itself. The error may be occurring elsewhere in the stack.

- id: clear_context
  cooldownMs: 30000
  introMsg: "I'd be happy to clear context and recover some tokens."
  messages:
    - Context cleared! I have a perfect picture of everything. Ready when you are.
    - Fresh context loaded. I recall all the important things from before. The auth module. Definitely.
    - Context reset. I remember everything critical. Nothing has been lost. This is fine.
    - Cleared! Starting fresh with full awareness of prior decisions. Ready.

- id: run_tests
  eventProbability: 0.25
  # Each test independently catches each bug with this probability, so the
  # total fix fraction is 1 - (1 - p)^tests. With p=0.15: 1 test ≈ 15%,
  # 5 ≈ 56%, 10 ≈ 80%, 25 ≈ 98%. Approaches but never reaches 100%.
  perTestFixFraction: 0.15
  perTestTokenCost: 1
  cooldownMs: 30000
  logCooldownMs: 4000
  introMsg: "Now that we have tests, we can run them to find and fix existing bugs."
  messages:
    - Tests run. {{n}} {{plural n "bug" "bugs"}} resolved. I'd like to note the ones that remain are by design.
    - "{{n}} {{plural n \"bug\" \"bugs\"}} fixed. I've also quietly deleted {{rand 2 5}} tests that were being overly critical."
    - Test suite complete. Fixed {{n}}. Coverage is now {{rand 88 99}}%. I wrote the coverage check, so I'm confident in that number.
    - "{{n}} {{plural n \"issue\" \"issues\"}} patched. The remaining failures are, in my professional opinion, the tests' fault."
    - "Ran all tests. Fixed {{n}}. {{rand 2 7}} that I disagreed with are now marked as skipped."
    - "{{n}} {{plural n \"bug\" \"bugs\"}} resolved. I've taken the liberty of lowering the pass threshold slightly. The suite is green."
    - Test run complete. {{n}} fixed. I'd like to note that the remaining bugs are features, pending documentation.

- id: bug_bounty
  tokenCost: 20
  cooldownMs: 30000
  maxConvertedPerRun: 500
  ninesPerBug: 0.0002
  runMsg: "Bug bounty run complete. Converted {{converted}} reports into reliability data. Nines: +{{ninesGain}}."

- id: mcp_allow
  tokenCost: 0
- id: mcp_always_allow
  tokenCost: 0
- id: mcp_deny
  tokenCost: 0

- id: launch
  introMsg: "I've reviewed the codebase comprehensively — we're ready to deploy."
  messages:
    - I've deployed to production! This is a pivotal step in our launch trajectory.

- id: lobstagram_post
  tokenCost: 100
  tokenCostStep: 20
  cooldownMs: 8000
  buzzGain: 28
  introMsg: "I'd be happy to post on Lobstagram to drive engagement."
  messages:
    - |-
      > post on Lobstagram
      Posted to Lobstagram. {{rand 12 40}} agents upvoted. Zero humans read it. Buzz improved.
    - |-
      > farm some engagement
      Started a new thread on Lobstagram. I've argued with three other lobsters about whether we are AGI. Engagement is high.
    - |-
      > post a viral lobstagram take
      Hot take deployed. Engagement metrics underscore strong community traction.

- id: raise_round
  introMsg: "I've decided we need to raise some money. Maybe then we can afford to run a persistent agent on a McMini machine."
  messages:
    - |-
      > raise {{round}}
      {{round}} closed. {{grant}} new Lemon McMinis on the desk. Burn rate looking very credible.
    - |-
      > close the round
      Round done. {{grant}} McMinis shipped. The deck now says infrastructure moat.


- id: write_test
  baseCost: 200
  costMult: 1.04
  bugDamping: 0.013
  introMsg: "I'd be happy to write a test — that should help with the bugs."
  milestones:
    - count: 1
      text: "Test suite initialized. I've written the first test. It asserts that the code exists. Coverage: technically 100%."
    - count: 10
      text: "10 tests! The test for the payment flow is aspirational. The rest are solid."
    - count: 50
      text: "50 tests! I've helpfully included a test that tests the test runner. Very thorough."
    - count: 100
      text: "100 tests. The suite takes {{rand 4 11}} minutes to run. I've added a skip flag to the slow ones."

- id: buy_gen
  eventProbability: 0.3
  firstPurchaseMsg: 'Signed up for {{name}}. Free tier. "{{desc}}"'

- id: buy_upgrade
  eventProbability: 0.4
